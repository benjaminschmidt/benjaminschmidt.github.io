[project]
name = "personal-website"
version = "0.0.0"
description = "The code for the personal website of Benjamin Schmidt using Django"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "django==6.0.8",
    "gunicorn==26.0.0",
    "pillow==12.3.0",
    "psycopg[binary]==3.3.4",
    "python-dotenv==1.2.2",
]

[tool.uv]
exclude-newer = "13 days"